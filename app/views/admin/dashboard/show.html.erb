<section class="admin-dashboard">
  <header class="page-header">
    <h1>Admin &raquo; Dashboard</h1>
  </header>

  <div class="dash-body">
    <% @counts.each do |key, value| %>
      <%= render 'item', key: key, value: value %>
    <% end %>
  </div>
</section>

